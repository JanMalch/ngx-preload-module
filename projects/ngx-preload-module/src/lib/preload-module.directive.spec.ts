import { PreloadModuleDirective } from './preload-module.directive';

describe('PreloadModuleDirective', () => {
  it('should create an instance', () => {
    const directive = new PreloadModuleDirective();
    expect(directive).toBeTruthy();
  });
});
